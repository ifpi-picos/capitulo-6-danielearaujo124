// Define a função que calcula o quadrado
function calcularQuadrado(numero) {
    return numero * numero;
  }
  
  // Chama a função e exibe o resultado no console
  const resultado = calcularQuadrado(5);
  console.log("O quadrado do número é: " + resultado);
  